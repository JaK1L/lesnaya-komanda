# 🚀 Инструкция по применению исправлений

## Шаг 1: Установка зависимостей

```bash
# Backend
cd backend
pip install slowapi==0.1.9 bleach==6.1.0 email-validator==2.1.0

# Или из файла
pip install -r requirements-additions.txt
```

## Шаг 2: Применение индексов БД

```bash
# Применить индексы (ВАЖНО!)
python backend/apply_performance_indexes.py

# Проверить что индексы созданы
# Должно показать ~30+ индексов
```

**Ожидаемый результат:**
```
✅ Индексы успешно созданы!
📈 Проверка созданных индексов:

📋 users:
  ✓ idx_users_discord_id
  ✓ idx_users_rating
  ✓ idx_users_last_seen
  ...

🎉 Всего создано индексов: 32
```

## Шаг 3: Смена пароля администратора

```bash
# В .env файле обновить:
ADMIN_USERNAME=your_new_username
ADMIN_PASSWORD=your_secure_password_min_32_chars

# Перезапустить backend - пароль обновится автоматически
```

## Шаг 4: Интеграция Rate Limiting

### 4.1 Обновить main.py

```python
# В backend/app/main.py добавить импорт:
from .rate_limit import setup_rate_limiting

# После создания app добавить:
setup_rate_limiting(app)
```

### 4.2 Добавить rate limiting в auth.py

```python
# В backend/app/routes/auth.py добавить импорт:
from ..rate_limit import limiter

# Добавить декоратор к login эндпоинту:
@router.post("/token", response_model=Token)
@limiter.limit("5/minute")  # Максимум 5 попыток входа в минуту
async def login_for_access_token(
    request: Request,  # ВАЖНО: добавить параметр request
    user_login: UserLogin,
    db: asyncpg.Connection = Depends(get_db)
):
    ...
```

### 4.3 Добавить rate limiting в другие критические эндпоинты

```python
# В backend/app/routes/admin.py:
from ..rate_limit import limiter

@router.post("/events")
@limiter.limit("10/minute")  # 10 событий в минуту
async def create_event(request: Request, ...):
    ...

@router.post("/news")
@limiter.limit("10/minute")
async def create_news(request: Request, ...):
    ...
```

## Шаг 5: Интеграция пагинации

### 5.1 Обновить admin.py

```python
# В backend/app/routes/admin.py добавить импорт:
from ..pagination import PaginationParams, PaginatedResponse
from fastapi import Query

# Обновить list_users:
@router.get("/users")
async def list_users(
    page: int = Query(1, ge=1),
    limit: int = Query(50, ge=1, le=100),
    db: asyncpg.Connection = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    offset = (page - 1) * limit
    
    rows = await db.fetch(
        """
        SELECT id, discord_id, discord_username, forest_rank, rating, 
               avatar_url, site_nickname, joined_at, last_seen
        FROM users
        ORDER BY last_seen DESC, id DESC
        LIMIT $1 OFFSET $2
        """,
        limit, offset
    )
    
    total = await db.fetchval("SELECT COUNT(*) FROM users")
    
    return PaginatedResponse.create(
        items=[UserOut(**dict(row)) for row in rows],
        total=total,
        page=page,
        limit=limit
    )
```

### 5.2 Аналогично обновить другие list эндпоинты

- `list_events`
- `list_news`
- `list_feed`
- `list_streamers`
- `list_merch`

## Шаг 6: Интеграция валидации

### 6.1 Обновить модели в admin.py

```python
# В backend/app/routes/admin.py добавить импорт:
from ..validation import ContentValidationMixin, validate_url, validate_telegram_url

# Обновить EventCreate:
class EventCreate(BaseModel, ContentValidationMixin):
    title: str = Field(..., max_length=200)
    description: str = Field(..., max_length=2000)
    game: Optional[str] = Field(default="Общее", max_length=50)
    event_date: datetime
    status: str = Field(default="Планируется", max_length=30)
    telegram_url: Optional[str] = Field(None, max_length=500)
    expires_at: Optional[datetime] = None
    
    @field_validator('telegram_url')
    @classmethod
    def validate_telegram(cls, v):
        return validate_telegram_url(v) if v else None

# Обновить NewsCreate:
class NewsCreate(BaseModel, ContentValidationMixin):
    title: str = Field(..., max_length=200)
    content: str = Field(..., max_length=5000)
    image_url: Optional[str] = Field(None, max_length=500)
    published: bool = True
    
    @field_validator('image_url')
    @classmethod
    def validate_image(cls, v):
        return validate_url(v) if v else None
```

## Шаг 7: Настройка логирования в production

### 7.1 Создать конфиг логирования

```python
# backend/app/logging_config.py
import logging
import sys
from logging.handlers import RotatingFileHandler

def setup_logging(debug: bool = False):
    """Настройка логирования"""
    level = logging.DEBUG if debug else logging.INFO
    
    # Формат логов
    formatter = logging.Formatter(
        '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )
    
    # Console handler
    console_handler = logging.StreamHandler(sys.stdout)
    console_handler.setFormatter(formatter)
    
    # File handler с ротацией
    file_handler = RotatingFileHandler(
        'logs/app.log',
        maxBytes=10*1024*1024,  # 10MB
        backupCount=5
    )
    file_handler.setFormatter(formatter)
    
    # Root logger
    root_logger = logging.getLogger()
    root_logger.setLevel(level)
    root_logger.addHandler(console_handler)
    root_logger.addHandler(file_handler)
    
    # Отключаем verbose логи от библиотек
    logging.getLogger('passlib').setLevel(logging.ERROR)
    logging.getLogger('asyncpg').setLevel(logging.WARNING)
```

### 7.2 Обновить main.py

```python
# Заменить basicConfig на:
from .logging_config import setup_logging

setup_logging(debug=settings.DEBUG)
logger = logging.getLogger(__name__)
```

## Шаг 8: Тестирование

### 8.1 Проверить индексы

```bash
# Запустить backend
cd backend
python -m app.main

# Проверить что индексы работают (в логах должно быть):
# ✅ Подключение к базе данных установлено
# ✅ База данных полностью готова к работе!
```

### 8.2 Проверить rate limiting

```bash
# Попробовать залогиниться 6 раз подряд
# 6-я попытка должна вернуть 429 Too Many Requests
curl -X POST http://localhost:8000/api/token \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"wrong"}'
```

### 8.3 Проверить пагинацию

```bash
# Запросить пользователей с пагинацией
curl http://localhost:8000/api/admin/users?page=1&limit=10 \
  -H "Authorization: Bearer YOUR_TOKEN"

# Ответ должен содержать:
# {
#   "items": [...],
#   "total": 100,
#   "page": 1,
#   "limit": 10,
#   "pages": 10
# }
```

### 8.4 Проверить валидацию

```bash
# Попробовать создать новость с XSS
curl -X POST http://localhost:8000/api/admin/news \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Test <script>alert(1)</script>",
    "content": "Content with <script>alert(1)</script>",
    "published": true
  }'

# <script> теги должны быть удалены
```

## Шаг 9: Мониторинг

### 9.1 Проверить логи

```bash
# Логи должны быть в формате:
# 2026-03-07 10:30:45 - backend.app.main - INFO - ✅ Подключение к базе данных установлено

# Проверить что нет print() в логах
grep "print(" backend/app/*.py
# Не должно быть результатов
```

### 9.2 Мониторинг производительности

```sql
-- Проверить что индексы используются
EXPLAIN ANALYZE SELECT * FROM users WHERE discord_id = 123456789;

-- Должно быть: Index Scan using idx_users_discord_id
-- НЕ должно быть: Seq Scan
```

## Шаг 10: Деплой

### 10.1 Обновить .env на production

```bash
# Обязательно обновить:
ADMIN_USERNAME=secure_username
ADMIN_PASSWORD=very_secure_password_at_least_32_characters
SECRET_KEY=new_secret_key_at_least_32_characters

# Для rate limiting с Redis (опционально):
REDIS_URL=redis://localhost:6379/0
```

### 10.2 Применить миграции

```bash
# На production сервере:
python backend/apply_performance_indexes.py
```

### 10.3 Перезапустить сервисы

```bash
# Backend
systemctl restart backend

# Frontend (если нужно)
systemctl restart frontend
```

## ✅ Чеклист

- [ ] Установлены зависимости (slowapi, bleach, email-validator)
- [ ] Применены индексы БД
- [ ] Сменен пароль администратора
- [ ] Интегрирован rate limiting
- [ ] Добавлена пагинация в list эндпоинты
- [ ] Добавлена валидация в модели
- [ ] Настроено логирование
- [ ] Протестирован rate limiting
- [ ] Протестирована пагинация
- [ ] Протестирована валидация
- [ ] Проверены логи
- [ ] Проверена производительность БД
- [ ] Обновлен .env на production
- [ ] Применены миграции на production
- [ ] Перезапущены сервисы

## 🆘 Troubleshooting

### Проблема: Индексы не применяются

```bash
# Проверить что индексы созданы
psql $DATABASE_URL -c "\di"

# Если нет - применить вручную
psql $DATABASE_URL < backend/migrations/add_performance_indexes.sql
```

### Проблема: Rate limiting не работает

```python
# Проверить что setup_rate_limiting вызван в main.py
# Проверить что request параметр добавлен в эндпоинты
# Проверить логи на наличие ошибок
```

### Проблема: Валидация не работает

```python
# Проверить что ContentValidationMixin добавлен в модели
# Проверить что field_validator импортирован из pydantic
# Проверить версию pydantic (должна быть 2.x)
```

---

**Время применения:** ~30-60 минут  
**Сложность:** Средняя  
**Требуется перезапуск:** Да
